pre-commit:
  commands:
    biome:
      glob: "**/*.{ts,tsx,js,jsx,json}"
      run: biome check --apply {staged_files}
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: bun run check-types
      skip: merge
